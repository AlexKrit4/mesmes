[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "node --experimental-sqlite --no-warnings server/index.js"
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"
